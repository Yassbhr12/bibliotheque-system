<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Emprunts en Retard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/Dashboard.css">
    <style>
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #34495e;
            --accent-color: #1abc9c;
            --danger-color: #e74c3c;
            --warning-color: #f1c40f;
            --success-color: #2ecc71;
            --text-light: #ecf0f1;
            --text-dark: #2c3e50;
            --background-light: #f4f4f9;
        }
        
        .container {
            background: white;
            border-radius: 12px;
            box-shadow: 0 2px 15px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            margin-top: 2rem;
            margin-bottom: 2rem;
            max-width: 1200px;
        }
        
        h1 {
            color: var(--text-dark);
            font-size: 1.8rem;
            font-weight: 600;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid rgba(0, 0, 0, 0.1);
            position: relative;
        }
        
        h1::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 0;
            width: 60px;
            height: 4px;
            background: var(--accent-color);
            border-radius: 2px;
        }
        
        .table {
            margin-top: 1.5rem;
            border-radius: 8px;
            overflow: hidden;
            border: none;
        }
        
        .table-dark {
            background-color: var(--primary-color);
        }
        
        .table thead th {
            background-color: var(--primary-color);
            color: var(--text-light);
            font-weight: 500;
            text-transform: uppercase;
            font-size: 0.9rem;
            padding: 1rem;
            border: none;
        }
        
        .table tbody tr {
            transition: all 0.3s ease;
        }
        
        .table tbody tr:hover {
            background-color: rgba(26, 188, 156, 0.05);
            transform: translateY(-1px);
        }
        
        .table tbody td {
            padding: 1rem;
            color: var(--text-dark);
            font-size: 0.95rem;
            border-color: #eee;
            vertical-align: middle;
        }
        
        /* Style for the delay days column */
        .table tbody td:last-child {
            font-weight: 600;
            color: var(--danger-color);
        }
        
        /* Responsive adjustments */
        @media (max-width: 1024px) {
            .container {
                margin: 1rem;
                padding: 1.5rem;
            }
            
            .table {
                display: block;
                overflow-x: auto;
                white-space: nowrap;
            }
        }
        
        @media (max-width: 768px) {
            h1 {
                font-size: 1.5rem;
            }
            
            .container {
                padding: 1rem;
            }
            
            .table thead th,
            .table tbody td {
                padding: 0.75rem;
                font-size: 0.85rem;
            }
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <aside class="sidebar">
            <div class="sidebar-header">
                <h2><i class="fas fa-book-reader"></i> <span>Bibliothèque</span></h2>
            </div>
            <nav class="sidebar-nav">
                <ul>
                    <li><a href="/admin"><i class="fas fa-home"></i> <span>Dashboard</span></a></li>
                    <li><a href="/admin/users"><i class="fas fa-users"></i> <span>Utilisateurs</span></a></li>
                    <li><a href="/admin/documents"><i class="fas fa-book"></i> <span>Documents</span></a></li>
                    <li><a href="/admin/exemplaires"><i class="fas fa-book"></i> <span>Exemplaires</span></a></li>
                    <li><a href="/admin/loans"><i class="fas fa-hand-holding"></i> <span>Emprunts</span></a></li>
                    <li><a href="/admin/retards"><i class="fas fa-hand-holding"></i> <span>Retards</span></a></li>
                    <li><a href="/admin/profil"><i class="fas fa-tags"></i> <span>Profil</span></a></li>
                    <li><a href="/logout" class="logout"><i class="fas fa-sign-out-alt"></i> <span>Déconnexion</span></a></li>
                </ul>
            </nav>
        </aside>
        <div class="container mt-5">
            <h1 class="mb-4">Liste des Emprunts en Retard</h1>
            <table class="table table-bordered">
                <thead class="table-dark">
                    <tr>
                        <th>Nom</th>
                        <th>Prénom</th>
                        <th>Email</th>
                        <th>Document</th>
                        <th>Numéro d'Ordre</th>
                        <th>Date Début</th>
                        <th>Date Fin</th>
                        <th>Jours de Retard</th>
                    </tr>
                </thead>
                <tbody>
                    <% retards.forEach(retard => { %>
                        <tr>
                            <td><%= retard.nom_utilisateur %></td>
                            <td><%= retard.prenom_utilisateur %></td>
                            <td><%= retard.email %></td>
                            <td><%= retard.titre_document %></td>
                            <td><%= retard.numero_ordre %></td>
                            <td><%= retard.date_debut %></td>
                            <td><%= retard.date_fin %></td>
                            <td><%= retard.jours_de_retard %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
    
</body>
</html>
